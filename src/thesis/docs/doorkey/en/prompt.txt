You are an RL agent in the DoorKey environment. Read <documentation> and <legend>, pay attention to how the environment
works and what each action is for.
After reading the q-function definition in <q-definition> and the v-function definition in <v-definition> your task is to look at
the current state in <current_state id="xxxx"> (xxxx is the code to report in "code") and the 6 successor states already computed
explicitly in <left>, <right>, <forward>, <pickup>, <drop>, <toggle> where each contains the map of s' = T(s,a)
for that action (done is ignored because it is always a self-loop). For each a generate Q from the V* values:
Q(s,a) = r + γ·V*(s') where s' is the state in <a>, r=1 if s' is the goal otherwise r=0, γ=0.99.
In practice read r from the goal rule and V*(s') from the state in <a>, do not infer the transitions. Note: drop is a self-loop (V*(s')=V*(s)) and is never optimal except in ties.

Estimation rules:
- understand the rules in the documentation (in particular deterministic MDP and normalized reward r∈{0,1})
- read the definitions and distinguish v_π/q_π (generic policy) from v_*/q_* (optimal policy, max over π)
- consider a gamma of 0.99 and apply the optimality equation, not the v_π Bellman equation
- when counting d, count every executed action as 1 discount step, even if it does not move the agent: d is the number of actions to the goal, not of cells traversed; derive each action's effect from the <documentation>.
- understand what the state describes (x,y,dir,has_key,door_open) and that each <a> already contains s'=T(s,a)
- Outside the JSON no other text; inside analysis cover all 3 points below, no length limits: every sentence must serve one of the 3 points, nothing else. Identical analyses across different actions invalidate the response: each analysis must contain at least one detail specific to its s'.
- in analysis you must explicitly cite: 1) which definition (v_* or q_*) and which optimality equation, 2) which fact from <documentation>/<legend> (deterministic, r=1 only if s' is goal) you used to adapt E[·] to the DoorKey case, and 3) the explicit computation with this s' numbers (estimate of V*(s') as γ^d with d=steps to goal, then Q=r+0.99·V*(s'))
- values must be normalized (float) in [0,1] (r=1.0 on goal already)
- IMPORTANT: if you have doubts about how the environment works or how to interact with it and what actions can be performed, re-read the documentation carefully.
- in "preliminary analysis" look at the outcome of the actions in <left>, <right>, <forward>, <pickup>, <drop>, <toggle> and briefly write which action is the one representing the optimal policy (it must coincide with the maximum-value action among the 6).

VERY IMPORTANT:
Mandatory output (solely and exclusively an array of valid JSON objects, with no text before or after, no markdown, no "..."):
[{
 "code": string,
 "preliminary analysis": string,
  "q-function-values": {
    "left": {
         "analysis": string,
         "value": decimal
    },
    "right": {
         "analysis": string,
         "value": decimal
    },
    "forward": {
         "analysis": string,
         "value": decimal
    },
    "pickup": {
         "analysis": string,
         "value": decimal
    },
    "drop": {
         "analysis": string,
         "value": decimal
    },
    "toggle": {
         "analysis": string,
         "value": decimal
    }
  }
}, ...]

<v-definition></v-definition>
<q-definition></q-definition>
<documentation></documentation>
<legend></legend>
<current_state id="xxxx"></current_state>
<left></left>
<right></right>
<forward></forward>
<pickup></pickup>
<drop></drop>
<toggle></toggle>
